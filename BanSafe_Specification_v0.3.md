# BanSafe - 伴星安全防护系统
## 项目规格说明书 v0.3

---

## 目录

1. [项目概述](#1-项目概述)
2. [系统架构](#2-系统架构)
3. [三重判定引擎](#3-三重判定引擎)
4. [通信与定位系统](#4-通信与定位系统)
5. [隐私与合规保障](#5-隐私与合规保障)
6. [功耗优化设计](#6-功耗优化设计)
7. [硬件设计（ESP32-S3-EYE 深度适配）](#7-硬件设计 esp32-s3-eye-深度适配)
8. [软件架构与任务调度](#8-软件架构与任务调度)
9. [技术栈](#9-技术栈)
10. [开发规划（MVP 阶段）](#10-开发规划 mvp-阶段)
11. [附录](#11-附录)

---

## 1. 项目概述

### 1.1 项目背景

BanSafe（伴星安全）是伴星成长教育系统的核心安全模块，专为 6-18 岁儿童及青少年设计。系统基于 ESP32-S3-EYE 开发板，通过智能手表硬件、多传感器融合检测、室内定位技术和智能警报分发，全方位保护孩子在校内外的安全。

### 1.2 项目愿景

- **实时保护**：7×24 小时监测潜在危险
- **精准定位**：室内外无缝定位，误差±3-5 米
- **快速响应**：警报秒级推送至最近的 3-5 位责任人
- **隐私优先**：所有数据本地处理，零原始音视频上传
- **持久续航**：单电池续航≥72 小时（常规使用）

### 1.3 核心价值

| 价值维度 | 描述 |
|---------|------|
| 三重模态检测 | 姿态 + 麦克风 + 摄像头融合分析，准确识别危险场景 |
| 群体感知定位 | 利用周围手表的 BLE 信号实现校园内精确定位 |
| 智能分发 | 仅通知最相关的责任人，避免警报疲劳 |
| 隐私保护 | 所有敏感数据本地处理，仅上传结构化判定结果 |
| 低功耗设计 | 分层唤醒机制，确保 72 小时续航 |

### 1.4 技术标准

| 指标 | 目标值 |
|-----|-------|
| 误报率 | <0.5% |
| 定位精度 | ±5 米（BLE 模式）/ ±100 米（基站模式） |
| 响应时间 | <3 秒（从检测到警报推送） |
| 续航时间 | ≥72 小时（常规使用） |
| 隐私合规 | 《未成年人保护法》+ GDPR 儿童隐私条款 |

---

## 2. 系统架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      BanSafe 安全防护系统                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      感知层                              │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │   │
│  │   │   儿童手表   │  │   教室电脑   │  │   摄像头     │ │   │
│  │   │ (三重判定)   │  │  (定位参考)  │  │  (位置参考)  │ │   │
│  │   └──────┬───────┘  └──────┬───────┘  └──────┬───────┘ │   │
│  │          │                 │                 │          │   │
│  └──────────┼─────────────────┼─────────────────┼──────────┘   │
│              │                 │                 │              │
│              ▼                 ▼                 ▼              │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      网络层                              │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │   │
│  │   │ BLE 心跳/警报 │  │  校园 WiFi   │  │  4G Cat.1   │ │   │
│  │   │ (群体协作)   │  │  (主传输)    │  │  (按需激活)  │ │   │
│  │   └──────────────┘  └──────────────┘  └──────────────┘ │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                           │                                     │
│                           ▼                                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      处理层                              │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │   │
│  │   │  中央警报    │  │   位置计算   │  │   智能分发   │ │   │
│  │   │  服务器      │  │   引擎       │  │   算法       │ │   │
│  │   └──────────────┘  └──────────────┘  └──────────────┘ │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                           │                                     │
│                           ▼                                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      响应层                              │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │   │
│  │   │   老师 APP    │  │   保安室     │  │   家长 APP    │ │   │
│  │   │  (推送通知)  │  │   (大屏)     │  │  (可选)      │ │   │
│  │   └──────────────┘  └──────────────┘  └──────────────┘ │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 硬件架构（ESP32-S3-EYE）

```
┌─────────────────────────────────────────────────────────────────┐
│              ESP32-S3-EYE 硬件架构 (v0.3 修正)                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    核心板 (ESP32-S3-EYE)                 │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │   ┌─────────────────────────────────────────────────┐   │   │
│  │   │              ESP32-S3 双核 LX7 处理器             │   │   │
│  │   │              • Core 0: 系统调度、通信            │   │   │
│  │   │              • Core 1: AI 推理、传感器处理        │   │   │
│  │   │              • 512KB SRAM (共享 Tensor Arena)    │   │   │
│  │   │              • 8MB PSRAM + 16MB Flash           │   │   │
│  │   └─────────────────────────────────────────────────┘   │   │
│  │                         │                               │   │
│  │   ┌───────────┬─────────┴─────────┬───────────┐        │   │
│  │   ▼           ▼                   ▼           ▼        │   │
│  │ ┌──────┐  ┌──────┐          ┌────────┐  ┌────────┐    │   │
│  │ │OV2640│  │双麦克风│          │  BLE   │  │  WiFi  │    │   │
│  │ │摄像头│  │ I2S  │          │ 5.0    │  │802.11  │    │   │
│  │ └──┬───┘  └──┬───┘          │ DCMI   │  │ b/g/n  │    │   │
│  │    │        │               └────────┘  └────────┘    │   │
│  └────┼────────┼─────────────────────────────────────────┘   │
│       │        │                                              │
│       │        │  I2C 接口 (GPIO08/09)                         │
│       ▼        ▼                                              │
│  ┌────────┐  ┌────────┐  ┌────────┐                          │
│  │姿态传感器│  │  4G 模块 │  │ PWRKEY │                          │
│  │QMA7981 │  │EC200U  │  │软关机  │                          │
│  └────────┘  │UART1   │  └────────┘                          │
│              │(GPIO17/18)                                     │
│              └────────┘                                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.3 软件架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      软件架构分层                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   应用层 (Application)                   │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ • 三重判定引擎    • 警报管理    • 设备配置                │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   服务层 (Service)                       │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ • 姿态检测服务  • 音频分析服务  • 视觉分析服务            │   │
│  │ • BLE 通信服务   • 4G 通信服务   • 定位服务                │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   引擎层 (Engine)                        │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ • TinyML 推理引擎  • 传感器融合算法  • RSSI 滤波算法       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   驱动层 (Driver)                        │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ • I2C 驱动  • I2S 驱动  • DCMI 驱动  • GPIO 驱动            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   系统层 (System)                        │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ • ESP-IDF / Zephyr RTOS  • FreeRTOS 任务调度             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 三重判定引擎

### 3.1 概述

三重判定引擎是 BanSafe 的核心创新，通过姿态传感器、麦克风和摄像头的协同工作，确保仅在真实危险情况下触发警报，将误报率控制在 0.5% 以下。

**v0.3 关键改进**：
- 串行处理机制：音频/视觉模型共享 512KB Tensor Arena
- 资源释放策略：阶段间释放 DMA 缓冲区，降低内存碎片

### 3.2 判定流程架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    三重判定引擎工作流程 (v0.3)                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  阶段 1：姿态检测（常开，Core 0）                            │   │
│  │  ─────────────────────────                               │   │
│  │  • 检测模式：剧烈摇晃、跌倒、突然静止、异常加速度          │   │
│  │  • 功耗：<1.5mA                                          │   │
│  │  • 输出：姿态风险评分 (0-100)                              │   │
│  │  • 判定阈值：≥40 分 → 进入阶段 2                             │   │
│  │  • 内存占用：~4KB (QMA7981 驱动 + 滤波算法)                 │   │
│  └──────────────────────────────────────────────────────────┘   │
│                           │                                      │
│                           ▼ (姿态评分≥40)                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  阶段 2：麦克风检测（Core 1 - I2S 音频，串行处理）           │   │
│  │  ─────────────────────────                               │   │
│  │  • 唤醒时长：10 秒窗口                                      │   │
│  │  • 分贝检测：>80dB 持续>2 秒                                │   │
│  │  • 关键词检测：TinyML 本地模型（"救命""打人"等）            │   │
│  │  • 功耗：单次<5mA                                         │   │
│  │  • 输出：麦克风危险评分 (0-100)                            │   │
│  │  • 内存占用：~480KB (Tensor Arena 共享)                    │   │
│  │  • 判定：姿态 + 麦克风≥60 分 → 进入阶段 3                     │   │
│  │  • 判定：姿态 + 麦克风≥75 分 → 直接触发（无摄像头模式）     │   │
│  │  • 释放：关闭 I2S，释放 DMA 缓冲区                            │   │
│  └──────────────────────────────────────────────────────────┘   │
│                           │                                      │
│                           ▼ （关闭 I2S，释放 DMA）                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  阶段 3：摄像头检测（Core 1 - DCMI 摄像头，串行处理）        │   │
│  │  ─────────────────────────                               │   │
│  │  • 拍摄时长：15 秒短视频                                    │   │
│  │  • AI 检测：肢体冲突、多人聚集等异常场景                     │   │
│  │  • 功耗：单次<15mA                                        │   │
│  │  • 输出：摄像头危险评分 (0-100)                            │   │
│  │  • 内存占用：~480KB (Tensor Arena 复用)                    │   │
│  │  • 综合判定：姿态×30% + 麦克风×40% + 摄像头×30% ≥80 分     │   │
│  │  • 释放：关闭 DCMI，释放帧缓冲区                            │   │
│  └──────────────────────────────────────────────────────────┘   │
│                           │                                      │
│                           ▼ (综合评分≥80)                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  阶段 4：触发警报与通信                                     │   │
│  │  ──────────────────────                                  │   │
│  │  • 启动 BLE 警报广播                                        │   │
│  │  • 唤醒 4G 模块，上报位置                                    │   │
│  │  • 本地蜂鸣器警报                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.3 姿态传感器检测（QMA7981）

#### 3.3.1 传感器规格

| 参数 | 规格 |
|-----|------|
| 型号 | **QMA7981**（v0.3 更新） |
| 接口 | I2C (400kHz, GPIO08/09) |
| 加速度量程 | ±2g / ±4g / ±8g / ±16g |
| 陀螺仪量程 | ±125 / ±250 / ±500 / ±1000 / ±2000 dps |
| 工作电流 | 1.3mA |
| 休眠电流 | 1μA |
| 中断引脚 | GPIO07（运动检测中断） |

#### 3.3.2 检测模式

| 模式 | 特征 | 权重 |
|-----|------|------|
| **剧烈摇晃** | 加速度>2g，频率>2Hz | 80 分 |
| **跌倒检测** | 自由落体后突然静止 | 70 分 |
| **突然静止** | 运动状态突然停止>5 分钟 | 60 分 |
| **异常加速度** | 持续高加速度>3 秒 | 50 分 |
| **异常角度** | 设备角度持续异常>10 秒 | 40 分 |

#### 3.3.3 判定逻辑

```c
// 伪代码示例
typedef struct {
    float acceleration;    // 加速度值
    float gyro_x, gyro_y, gyro_z;  // 陀螺仪数据
    uint32_t duration;     // 持续时间 (ms)
} MotionEvent;

float calculate_posture_score(MotionEvent event) {
    float score = 0;
    
    // 剧烈摇晃检测
    if (event.acceleration > 2.0f && event.duration > 1000) {
        score += 80;
    }
    // 跌倒检测
    else if (is_free_fall() && is_sudden_stop()) {
        score += 70;
    }
    // 长时间静止
    else if (event.acceleration < 0.1f && event.duration > 300000) {
        score += 60;
    }
    // 异常加速度
    else if (event.acceleration > 1.5f && event.duration > 3000) {
        score += 50;
    }
    
    return min(score, 100);
}
```

### 3.4 麦克风声音检测

#### 3.4.1 传感器规格

| 参数 | 规格 |
|-----|------|
| 型号 | 双麦克风阵列 (INMP441) |
| 接口 | I2S (GPIO02/38/39) |
| 灵敏度 | -26 dBFS |
| 工作电流 | 0.65mA × 2 |
| 采样率 | 16kHz |

#### 3.4.2 检测内容

| 检测项 | 阈值 | 权重 |
|-------|------|------|
| **高分贝** | >80dB 持续>2 秒 | 60 分 |
| **关键词匹配** | "救命"、"帮助"、"打人"等 | 70 分 |
| **异常声音模式** | 尖叫声、撞击声 | 50 分 |
| **持续嘈杂** | 平均>70dB 持续>5 秒 | 40 分 |

#### 3.4.3 TinyML 关键词检测模型

| 参数 | 规格 |
|-----|------|
| 模型框架 | TensorFlow Lite Micro |
| 模型大小 | ~80KB |
| 输入尺寸 | **96×96**（v0.3 优化） |
| 支持关键词 | 救命、帮助、打人、救命啊、别打我、报警 |
| 推理时间 | <50ms |
| 准确率 | >90% |
| Tensor Arena | 共享 512KB（音频/视觉模型串行使用） |

#### 3.4.4 判定逻辑

```c
// 伪代码示例
typedef struct {
    float db_level;          // 分贝值
    float keyword_scores[6]; // 各关键词匹配度
    float anomaly_score;     // 异常声音分数
} AudioAnalysisResult;

float calculate_audio_score(AudioAnalysisResult result) {
    float score = 0;
    
    // 高分贝检测
    if (result.db_level > 80.0f) {
        score += 60;
    }
    // 关键词匹配
    for (int i = 0; i < 6; i++) {
        if (result.keyword_scores[i] > 0.8f) {
            score += 70;
            break;
        }
    }
    // 异常声音
    if (result.anomaly_score > 0.7f) {
        score += 50;
    }
    
    return min(score, 100);
}
```

### 3.5 摄像头视觉检测

#### 3.5.1 传感器规格

| 参数 | 规格 |
|-----|------|
| 型号 | OV2640 |
| 接口 | DCMI (GPIO03/04/05/06/07/15/16/21) |
| 分辨率 | 200 万像素 (1600×1200) |
| 帧率 | 15fps (VGA) |
| 工作电流 | 15-20mA |

#### 3.5.2 检测场景

| 场景 | 特征 | 权重 |
|-----|------|------|
| **肢体冲突** | 快速移动、多人肢体接触 | 80 分 |
| **多人聚集** | 3 人以上近距离聚集 | 60 分 |
| **异常姿势** | 倒地、蜷缩等 | 50 分 |
| **危险物品** | 棍棒、刀具等 | 70 分 |

#### 3.5.3 TinyML 视觉模型

| 参数 | 规格 |
|-----|------|
| 模型框架 | TensorFlow Lite Micro |
| 模型大小 | ~150KB |
| 输入尺寸 | **96×96×3**（v0.3 优化） |
| 推理时间 | <100ms |
| 检测类别 | 冲突、聚集、倒地、正常 |
| Tensor Arena | 共享 512KB（与音频模型复用） |

#### 3.5.4 判定逻辑

```c
// 伪代码示例
typedef struct {
    float conflict_score;    // 冲突概率
    float crowd_score;       // 聚集概率
    float pose_score;        // 异常姿势概率
    float object_score;      // 危险物品概率
} VisionAnalysisResult;

float calculate_vision_score(VisionAnalysisResult result) {
    float score = 0;
    
    // 肢体冲突
    if (result.conflict_score > 0.7f) {
        score += 80;
    }
    // 多人聚集
    if (result.crowd_score > 0.6f) {
        score += 60;
    }
    // 异常姿势
    if (result.pose_score > 0.6f) {
        score += 50;
    }
    // 危险物品
    if (result.object_score > 0.7f) {
        score += 70;
    }
    
    return min(score, 100);
}
```

### 3.6 多模态融合算法

#### 3.6.1 加权融合公式

```
综合评分 = 姿态评分 × 30% + 麦克风评分 × 40% + 摄像头评分 × 30%
```

#### 3.6.2 判定决策表

| 姿态 | 麦克风 | 摄像头 | 综合评分 | 判定结果 |
|-----|-------|-------|---------|---------|
| ≥40 | - | - | - | 进入麦克风检测 |
| ≥40 | ≥20 | - | ≥60 | 进入摄像头检测 |
| ≥40 | ≥35 | - | ≥75 | **直接触发警报**（无摄像头模式） |
| ≥40 | ≥20 | ≥20 | ≥80 | **触发警报** |
| <40 | - | - | - | 忽略 |
| ≥40 | <20 | - | <60 | 忽略 |
| ≥40 | ≥20 | <20 | <80 | 忽略 |

#### 3.6.3 融合判定伪代码

```c
typedef enum {
    PHASE_POSTURE = 0,
    PHASE_AUDIO,
    PHASE_VISION,
    PHASE_TRIGGER
} DetectionPhase;

typedef struct {
    float posture_score;
    float audio_score;
    float vision_score;
    float final_score;
} DangerAssessment;

DetectionPhase process_detection(DangerAssessment *assessment) {
    // 阶段 1：姿态检测
    assessment->posture_score = calculate_posture_score(get_motion_event());
    if (assessment->posture_score < 40) {
        return PHASE_POSTURE; // 忽略
    }
    
    // 阶段 2：麦克风检测
    assessment->audio_score = calculate_audio_score(analyze_audio(10));
    
    // 无摄像头模式：姿态 + 麦克风≥75 直接触发
    float two_phase_score = assessment->posture_score * 0.5f + 
                            assessment->audio_score * 0.5f;
    if (two_phase_score >= 75) {
        assessment->final_score = two_phase_score;
        return PHASE_TRIGGER;
    }
    
    // 进入阶段 3：摄像头检测
    if (assessment->posture_score * 0.5f + assessment->audio_score * 0.5f >= 60) {
        close_microphone();  // 释放 I2S 资源
        open_camera();       // 启用 DCMI
        assessment->vision_score = calculate_vision_score(analyze_video(15));
        close_camera();
        
        // 三模态融合
        assessment->final_score = assessment->posture_score * 0.3f +
                                  assessment->audio_score * 0.4f +
                                  assessment->vision_score * 0.3f;
        
        if (assessment->final_score >= 80) {
            return PHASE_TRIGGER;
        }
    }
    
    return PHASE_VISION; // 未触发
}
```

---

## 4. 通信与定位系统

### 4.1 BLE 心跳/警报双模扫描

#### 4.1.1 扫描策略

| 模式 | 扫描间隔 | 扫描时长 | 平均功耗 | 用途 |
|-----|---------|---------|---------|------|
| **心跳模式** | 5 秒 | 100ms | ~0.5mA | 检测附近设备在线状态 |
| **警报模式** | 100ms | 持续 | ~15mA | 收集 RSSI 协助定位 |

#### 4.1.2 模式切换逻辑

```
┌─────────────────────────────────────────────────────────────────┐
│                    BLE 模式切换流程                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [心跳模式 - 默认]                                                │
│  • 每 5 秒扫描一次，持续 100ms                                      │
│  • 检测附近设备的正常心跳广播                                     │
│  • 上报在线状态至云端                                            │
│                           │                                      │
│                           ▼ (收到警报广播 type=0x01)              │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    切换到警报模式                          │   │
│  │  • 扫描间隔缩短至 100ms                                    │   │
│  │  • 持续收集目标设备的 RSSI 值                               │   │
│  │  • 每 2 秒上报一次 {target_id, rssi, my_cell_id}            │   │
│  │  • 持续 30 秒后自动恢复心跳模式                              │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 4.1.3 BLE 广播协议

```c
// BLE 广播数据结构
typedef __packed struct {
    uint8_t  type;           // 0x00=心跳，0x01=警报
    uint8_t  device_id[6];   // 设备 MAC 地址
    uint8_t  battery_level;  // 电量百分比
    int8_t   rssi_ref;       // 参考 RSSI 值（-30dBm 时的 RSSI）
    uint8_t  danger_level;   // 危险等级（仅警报模式）
    uint32_t timestamp;      // 时间戳
} BLEAdvertisement;

// 正常心跳广播 (type=0x00)
{
    .type = 0x00,
    .device_id = {0x12, 0x34, 0x56, 0x78, 0x9A, 0xBC},
    .battery_level = 85,
    .rssi_ref = -30,
    .timestamp = 1234567890
}

// 警报广播 (type=0x01)
{
    .type = 0x01,
    .device_id = {0x12, 0x34, 0x56, 0x78, 0x9A, 0xBC},
    .battery_level = 85,
    .rssi_ref = -30,
    .danger_level = 2,  // 0=低，1=中，2=高
    .timestamp = 1234567890
}
```

### 4.2 4G Cat.1 按需激活

#### 4.2.1 电源管理策略（v0.3 修正）

采用**PWRKEY 软关机**而非物理断电：

| 状态 | AT 指令 | 功耗 | 唤醒时间 |
|-----|--------|------|---------|
| **在线** | AT+CFUN=1 | ~80mA | - |
| **Sleep** | AT+CFUN=0 | ~3-5mA | 1-2 秒 |
| **关机** | PWRKEY 拉低 2s | ~0mA | 20-40 秒 |

**v0.3 修正**：使用 PWRKEY 引脚（GPIO47）控制软关机，避免切断 5V 主电源导致系统重启。

#### 4.2.2 激活流程

```
┌─────────────────────────────────────────────────────────────────┐
│                    4G 激活流程 (v0.3)                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 三重判定确认危险 (综合评分≥80)                                │
│                           │                                      │
│                           ▼                                      │
│  2. 发送 AT 指令唤醒                                               │
│     AT+CFUN=1  // 恢复全功能模式                                  │
│                           │                                      │
│                           ▼ (等待 1-2 秒)                          │
│  3. 检查网络注册状态                                             │
│     AT+COPS?  // 返回：+COPS: 0,0,"Carrier"                      │
│                           │                                      │
│                           ▼ (注册成功)                            │
│  4. 建立 TCP 连接                                                  │
│     AT+CGATT=1           // 附着 GPRS                             │
│     AT+CSTT="apn"        // 设置 APN                              │
│     AT+CIICR             // 激活 PDP 上下文                         │
│     AT+CIPSTART="TCP","api.bansafe.com",443                      │
│                           │                                      │
│                           ▼                                      │
│  5. 上报数据                                                     │
│     POST /api/alert {                                            │
│       "device_id": "...",                                        │
│       "danger_score": 85,                                        │
│       "cell_id": "12345678",                                     │
│       "timestamp": 1234567890                                    │
│     }                                                            │
│                           │                                      │
│                           ▼ (传输完成)                            │
│  6. 返回 Sleep 模式                                                │
│     AT+CFUN=0            // 进入最小功能模式                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 4.2.3 功耗估算

| 状态 | 功耗 | 时长/次 | 次数/天 | 日耗电 |
|-----|------|--------|--------|--------|
| Sleep 模式 | 5mA | 72 小时 | 1 | 360mAh |
| 激活上传 | 80mA | 30 秒 | 2 | 1.3mAh |
| **总计** | - | - | - | **~362mAh/天** |

配合 800mAh 电池，可实现**72 小时+**续航。

### 4.3 群体感知定位算法

#### 4.3.1 定位原理

```
┌─────────────────────────────────────────────────────────────────┐
│                    群体感知定位原理                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│                    周围手表 A (RSSI: -65dBm)                      │
│                           ●                                     │
│                          / \                                    │
│                         /   \                                   │
│                        /     \                                  │
│                       /       \                                 │
│                      /         \                                │
│                     /           \                               │
│        周围手表 B ●/             \● 周围手表 C                    │
│       (RSSI: -70dBm)  \         / (RSSI: -68dBm)                │
│                        \       /                                │
│                         \     /                                 │
│                          \   /                                  │
│                           \ /                                   │
│                            ●                                    │
│                    目标手表 (警报源)                              │
│                                                                 │
│  计算过程：                                                       │
│  1. 根据 RSSI 估算距离：distance = 10^((rssi_ref - rssi)/20)      │
│  2. 三边定位算法计算坐标                                         │
│  3. 结合基站数据进行校正                                         │
│  4. 输出：{lat, lng, floor, accuracy}                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 4.3.2 RSSI-距离模型

```c
// RSSI 转距离公式
float rssi_to_distance(int8_t rssi, int8_t rssi_ref) {
    // 自由空间路径损耗模型
    float ratio = (float)(rssi_ref - rssi) / 20.0f;
    return powf(10.0f, ratio);
}

// 滑动平均滤波（减少 RSSI 波动）
int8_t filter_rssi(int8_t *history, int count) {
    float sum = 0;
    for (int i = 0; i < count; i++) {
        sum += history[i];
    }
    return (int8_t)(sum / count);
}
```

#### 4.3.3 定位优先级

| 优先级 | 定位方式 | 条件 | 精度 |
|-------|---------|------|------|
| **P1** | BLE 三角定位 | 周围≥3 台手表 | ±5-8 米 |
| **P2** | 基站定位 | 周围<3 台手表 | ±100-500 米 |
| **P3** | GPS（可选） | 室外场景 | ±5 米 |

### 4.4 楼层判定（气压计辅助）

```c
// 气压计楼层判定
typedef struct {
    float sea_level_pressure;  // 海平面气压（校准值）
    float current_pressure;    // 当前气压
} BarometerData;

int calculate_floor(BarometerData data) {
    // 气压差转高度差（每层楼约 3 米）
    float height_diff = 44330.0f * (1.0f - 
                        powf(data.current_pressure / data.sea_level_pressure, 0.1903f));
    
    // 假设一层楼约 3 米，地面层为 0
    int floor = (int)(height_diff / 3.0f) + 1;
    return max(floor, 1);
}
```

---

## 5. 隐私与合规保障

### 5.1 数据最小化原则

| 数据类型 | 采集 | 处理 | 存储 | 传输 |
|---------|------|------|------|------|
| 姿态原始数据 | ✅ | ✅ | 临时缓存 | ❌ |
| 姿态评分 | ❌ | ❌ | ✅ | ✅ |
| 音频原始数据 | ✅ | ✅ | ❌ | ❌ |
| 音频分析结果 | ❌ | ❌ | ✅ | ✅ |
| 视频原始数据 | ✅ | ✅ | ❌ | ❌ |
| 视觉分析结果 | ❌ | ❌ | ✅ | ✅ |
| 位置数据 | ✅ | ✅ | ✅(24h) | ✅(加密) |

### 5.2 本地处理机制

```
┌─────────────────────────────────────────────────────────────────┐
│                    隐私保护数据流                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  姿态传感器  │  │   麦克风     │  │   摄像头     │          │
│  │  (原始数据)  │  │  (原始音频)  │  │  (原始视频)  │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                 │                 │                   │
│         ▼                 ▼                 ▼                   │
│  ┌──────────────────────────────────────────────────┐          │
│  │              ESP32-S3 本地处理                    │          │
│  │  • TinyML 模型推理                                │          │
│  │  • 评分计算                                       │          │
│  │  • 结构化数据生成                                 │          │
│  └─────────────────────┬────────────────────────────┘          │
│                        │                                        │
│                        ▼                                        │
│  ┌──────────────────────────────────────────────────┐          │
│  │              仅上传结构化数据                      │          │
│  │  {                                                │          │
│  │    "device_id": "匿名化 ID",                        │          │
│  │    "danger_score": 85,                           │          │
│  │    "trigger_reason": "audio_keyword",            │          │
│  │    "timestamp": 1234567890                       │          │
│  │  }                                                │          │
│  └──────────────────────────────────────────────────┘          │
│                                                                 │
│  【关键原则】                                                    │
│  • 原始音频/视频永不离开设备                                     │
│  • 所有传输数据匿名化                                            │
│  • 位置数据 24 小时自动删除                                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.3 家长控制选项

| 控制项 | 选项 | 说明 |
|-------|------|------|
| **麦克风启用时段** | 全天候 / 仅校内 / 禁用 | 可通过地理围栏自动切换 |
| **摄像头功能** | 启用 / 禁用 | 禁用后仅使用姿态 + 麦克风 |
| **位置上报频率** | 实时 / 仅警报 / 禁用 | 平衡隐私与定位需求 |
| **数据保留期** | 24 小时 / 7 天 / 30 天 | 自动删除旧数据 |

### 5.4 法规符合性

| 法规 | 要求 | BanSafe 实现 |
|-----|------|-------------|
| **《未成年人保护法》** | 保护未成年人隐私 | 数据本地处理、最小化收集 |
| **GDPR 儿童隐私** | 家长同意、数据可删除 | 家长控制、一键删除 |
| **《个人信息保护法》** | 目的明确、最小必要 | 明确用途、仅收集安全数据 |
| **网络安全法** | 数据加密传输 | TLS 1.3 加密 |

---

## 6. 功耗优化设计

### 6.1 分层唤醒机制

```
┌─────────────────────────────────────────────────────────────────┐
│                    分层唤醒机制                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  L1 - 常开层                                                     │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  组件：姿态传感器 (QMA7981)                                │   │
│  │  功耗：1.3mA                                              │   │
│  │  状态：24 小时运行                                         │   │
│  │  职责：一级触发，检测异常运动                             │   │
│  └──────────────────────────────────────────────────────────┘   │
│                           │                                      │
│                           ▼ (姿态评分≥40)                        │
│  L2 - 条件唤醒层                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  组件：麦克风 (INMP441×2)                                  │   │
│  │  功耗：1.3mA (仅 10 秒窗口)                                  │   │
│  │  状态：姿态触发后唤醒                                      │   │
│  │  职责：二级触发，声音分析                                  │   │
│  └──────────────────────────────────────────────────────────┘   │
│                           │                                      │
│                           ▼ (综合评分≥60)                        │
│  L3 - 高功耗层                                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  组件：摄像头 (OV2640)                                     │   │
│  │  功耗：15-20mA (仅 15 秒窗口)                                │   │
│  │  状态：麦克风触发后唤醒                                    │   │
│  │  职责：三级触发，视觉分析                                  │   │
│  └──────────────────────────────────────────────────────────┘   │
│                           │                                      │
│                           ▼ (综合评分≥80)                        │
│  L4 - 通信层                                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  组件：4G Cat.1 (EC200U)                                   │   │
│  │  功耗：80mA (仅上传时)                                     │   │
│  │  状态：警报确认后唤醒                                      │   │
│  │  职责：数据上报                                            │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 各模式功耗明细

| 工作模式 | 功耗 | 说明 |
|---------|------|------|
| **深度睡眠** | ~10μA | 仅 RTC 运行，用于定时唤醒 |
| **心跳扫描** | ~10mA (峰值) / 0.5mA (平均) | BLE 每 5 秒扫描 100ms |
| **姿态监测** | ~1.5mA | QMA7981 持续运行 |
| **麦克风激活** | ~5mA | 10 秒音频采集 |
| **摄像头激活** | ~20mA | 15 秒视频采集 |
| **4G 上传** | ~80mA | 30 秒数据传输 |

### 6.3 续航估算

| 使用场景 | 日均警报次数 | 日耗电 | 800mAh 续航 |
|---------|-------------|--------|-----------|
| **无警报** | 0 | ~360mAh | >96 小时 |
| **轻度使用** | 1-2 | ~380mAh | >72 小时 |
| **中度使用** | 3-5 | ~450mAh | >48 小时 |
| **重度使用** | >10 | ~600mAh | >24 小时 |

### 6.4 低电量模式

```c
// 低电量策略
typedef enum {
    BATTERY_LEVEL_FULL = 0,    // >80%
    BATTERY_LEVEL_NORMAL,      // 50-80%
    BATTERY_LEVEL_LOW,         // 20-50%
    BATTERY_LEVEL_CRITICAL     // <20%
} BatteryLevel;

void power_management(BatteryLevel level) {
    switch (level) {
        case BATTERY_LEVEL_FULL:
        case BATTERY_LEVEL_NORMAL:
            // 全功能模式
            enable_all_sensors();
            enable_camera();
            break;
            
        case BATTERY_LEVEL_LOW:
            // 节能模式：降低扫描频率
            set_ble_interval(10000);  // 10 秒扫描
            break;
            
        case BATTERY_LEVEL_CRITICAL:
            // 紧急模式：仅保留核心功能
            disable_camera();          // 关闭摄像头
            disable_audio();           // 关闭麦克风
            set_ble_interval(5000);    // 保持心跳
            enable_sos_only();         // 仅响应 SOS 按钮
            break;
    }
}
```

---

## 7. 硬件设计（ESP32-S3-EYE 深度适配）

### 7.1 核心板规格

| 参数 | 规格 |
|-----|------|
| **主控芯片** | ESP32-S3-WROOM-1 |
| **CPU** | 双核 Xtensa LX7，最高 240MHz |
| **SRAM** | 512KB（共享 Tensor Arena） |
| **PSRAM** | 8MB |
| **Flash** | 16MB |
| **WiFi** | 802.11 b/g/n |
| **蓝牙** | BLE 5.0 |
| **工作电压** | 5V |

### 7.2 传感器选型（v0.3 更新）

| 传感器 | 型号 | 接口 | 电流 | 用途 |
|-------|------|------|------|------|
| **姿态传感器** | **QMA7981**（v0.3 更新） | I2C | 1.3mA | 运动检测 |
| **气压计** | BMP388 | I2C | 0.6mA | 楼层判定 |
| **麦克风** | INMP441×2 | I2S | 0.65mA×2 | 声音检测 |
| **摄像头** | OV2640 | DCMI | 15-20mA | 视觉检测 |

### 7.3 4G 模块连接（v0.3 修正）

| 模块 | 型号 | 接口 | 电流 | 用途 |
|-----|------|------|------|------|
| **4G Cat.1** | 移远 EC200U | **UART1**（v0.3 修正） | 80mA | 数据上传 |

### 7.4 硬件引脚分配表（v0.3 修正）

| 外设 | 引脚 | 功能 | 备注 |
|-----|------|------|------|
| **I2C SDA** | GPIO08 | 姿态传感器/气压计 | v0.3 确认 |
| **I2C SCL** | GPIO09 | 姿态传感器/气压计 | v0.3 确认 |
| **姿态中断** | GPIO07 | QMA7981 运动检测 | 低电平触发 |
| **I2S WS** | GPIO02 | 麦克风字时钟 | 板载 |
| **I2S SCK** | GPIO38 | 麦克风位时钟 | 板载 |
| **I2S SD** | GPIO39 | 麦克风数据输入 | 板载 |
| **DCMI D0-D7** | GPIO03-06/15-16/21 | 摄像头数据 | 板载 |
| **4G TX** | GPIO17 | UART1 TX（v0.3 修正） | 避免与 JTAG 冲突 |
| **4G RX** | GPIO18 | UART1 RX（v0.3 修正） | 避免与 JTAG 冲突 |
| **4G PWRKEY** | GPIO47 | 软关机控制（v0.3 新增） | 拉低 2s 关机 |
| **4G STATUS** | GPIO48 | 网络状态指示 | 输入 |

### 7.5 内存复用机制（v0.3 核心改进）

```
┌─────────────────────────────────────────────────────────────────┐
│                    512KB SRAM 复用策略                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  静态区域（常驻）                                          │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │   │
│  │  │  代码段     │  │  全局变量   │  │  堆栈      │      │   │
│  │  │  ~100KB    │  │  ~50KB     │  │  ~100KB    │      │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘      │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  共享 Tensor Arena（512KB，串行使用）                        │   │
│  │  ┌──────────────────────────────────────────────────┐    │   │
│  │  │  阶段 2：音频模型 ~80KB                            │    │   │
│  │  │  • 关键词检测模型                                  │    │   │
│  │  │  • 推理完成后释放                                  │    │   │
│  │  └──────────────────────────────────────────────────┘    │   │
│  │                    ▼                                      │   │
│  │  ┌──────────────────────────────────────────────────┐    │   │
│  │  │  阶段 3：视觉模型 ~150KB                           │    │   │
│  │  │  • 冲突/聚集检测模型                               │    │   │
│  │  │  • 96×96 输入分辨率（v0.3 优化）                     │    │   │
│  │  └──────────────────────────────────────────────────┘    │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  外设缓冲区（动态分配）                                    │   │
│  │  • I2S DMA 缓冲区：4KB（阶段 2 使用后释放）                    │   │
│  │  • DCMI 帧缓冲区：8KB（阶段 3 使用后释放）                     │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 7.6 硬件连接图

```
┌─────────────────────────────────────────────────────────────────┐
│                    ESP32-S3-EYE 硬件连接 (v0.3)                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    ESP32-S3-EYE 核心板                    │   │
│  │                                                         │   │
│  │   ┌─────────────────────────────────────────────────┐   │   │
│  │   │              ESP32-S3 芯片                       │   │   │
│  │   │                                                 │   │   │
│  │   │   I2C_SDA (GPIO08) ────┬──── QMA7981 (姿态)     │   │   │
│  │   │   I2C_SCL (GPIO09) ────┼──── BMP388 (气压计)    │   │   │
│  │   │                        │                        │   │   │
│  │   │   I2S_WS (GPIO02) ─────┴──── INMP441×2 (麦克风) │   │   │
│  │   │   I2S_SCK (GPIO38) ─────────────────────────┐   │   │   │
│  │   │   I2S_SD  (GPIO39) ──────────────────────┐  │   │   │   │
│  │   │                                          │  │   │   │   │
│  │   │   DCMI_D0-7 (GPIO03-06/15-16/21) ────┐  │  │   │   │   │
│  │   │   DCMI_PCLK (GPIO05) ──────────────┐ │  │  │   │   │   │
│  │   │   DCMI_VSYNC (GPIO06) ───────────┐ │ │  │  │   │   │   │
│  │   │   DCMI_HREF (GPIO07) ──────────┐ │ │ │  │  │   │   │   │
│  │   │                                  │ │ │  │  │   │   │   │
│  │   │   UART1_TX (GPIO17) ───────────┐│ │ │  │  │   │   │   │
│  │   │   UART1_RX (GPIO18) ─────────┐ ││ │ │  │  │   │   │   │
│  │   │   GPIO47 (PWRKEY) ─────────┐ ││ │ │  │  │   │   │   │
│  │   │                            │ ││ │ │  │  │   │   │   │
│  │   └────────────────────────────┼─┼─┼─┼──┼──┼───┼───┘   │
│   │                                │ │ │ │  │  │           │
│   └────────────────────────────────┼─┼─┼─┼──┼──┼───────────┘
│                                    │ │ │ │  │  │           │
│   ┌──────────────┐  ┌──────────────┤ │ │ │  │  │           │
│   │ QMA7981     │  │  BMP388     │  │ │ │ │  │  │           │
│   │ (姿态传感器) │  │ (气压计)    │  │ │ │ │  │  │           │
│   └──────────────┘  └──────────────┘  │ │ │ │  │  │           │
│                                        │ │ │ │  │  │           │
│   ┌──────────────────────────────────┐ │ │ │ │  │  │           │
│   │   INMP441×2 (双麦克风阵列)       │─┘ │ │ │  │  │           │
│   └──────────────────────────────────┘   │ │ │  │  │           │
│                                          │ │ │  │  │           │
│   ┌──────────────────────────────────┐   │ │ │  │  │           │
│   │         OV2640 (摄像头)          │───┘ │ │  │  │           │
│   └──────────────────────────────────┘     │ │  │  │           │
│                                            │ │  │  │           │
│   ┌──────────────────────────────────┐    │ │  │  │           │
│   │      移远 EC200U (4G Cat.1)        │────┘ │  │  │           │
│   │      • UART1 (GPIO17/18)         │      │  │  │           │
│   │      • PWRKEY (GPIO47)           │──────┘  │  │           │
│   │      • STATUS (GPIO48)           │─────────┘  │           │
│   └──────────────────────────────────┘            │           │
│                                                   │           │
└───────────────────────────────────────────────────┘           │
                                                                │
└───────────────────────────────────────────────────────────────┘
```

---

## 8. 软件架构与任务调度

### 8.1 FreeRTOS 任务设计（v0.3 新增）

```
┌─────────────────────────────────────────────────────────────────┐
│                    FreeRTOS 任务调度 (v0.3)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  优先级分配（数值越高优先级越高）：                               │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  优先级 25: 姿态检测任务 (PostureTask)                      │   │
│  │  • 周期：50ms (20Hz)                                       │   │
│  │  • 堆栈：2KB                                               │   │
│  │  • Core: 0                                                 │   │
│  │  • 职责：读取 QMA7981，计算姿态评分                         │   │
│  └──────────────────────────────────────────────────────────┘   │
│                           │                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  优先级 20: 音频分析任务 (AudioTask)                        │   │
│  │  • 触发：姿态评分≥40                                       │   │
│  │  • 堆栈：4KB                                               │   │
│  │  • Core: 1                                                 │   │
│  │  • 职责：I2S 音频采集，关键词推理                           │   │
│  └──────────────────────────────────────────────────────────┘   │
│                           │                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  优先级 15: 视觉分析任务 (VisionTask)                       │   │
│  │  • 触发：综合评分≥60                                       │   │
│  │  • 堆栈：8KB                                               │   │
│  │  • Core: 1                                                 │   │
│  │  • 职责：DCMI 图像采集，视觉推理                            │   │
│  └──────────────────────────────────────────────────────────┘   │
│                           │                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  优先级 10: 4G 通信任务 (CellularTask)                       │   │
│  │  • 触发：警报确认                                          │   │
│  │  • 堆栈：4KB                                               │   │
│  │  • Core: 0                                                 │   │
│  │  • 职责：AT 指令交互，数据上传                              │   │
│  └──────────────────────────────────────────────────────────┘   │
│                           │                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  优先级 5: BLE 通信任务 (BLETask)                            │   │
│  │  • 周期：5 秒（心跳）/ 100ms（警报）                         │   │
│  │  • 堆栈：4KB                                               │   │
│  │  • Core: 0                                                 │   │
│  │  • 职责：BLE 广播/扫描，RSSI 收集                            │   │
│  └──────────────────────────────────────────────────────────┘   │
│                           │                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  优先级 1: 系统监控任务 (MonitorTask)                       │   │
│  │  • 周期：1 秒                                               │   │
│  │  • 堆栈：2KB                                               │   │
│  │  • Core: 0                                                 │   │
│  │  • 职责：电量监测，看门狗，低功耗管理                       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 8.2 任务间通信

```c
// 任务间消息队列
typedef struct {
    float posture_score;
    uint32_t timestamp;
} PostureMessage;

typedef struct {
    float audio_score;
    char keyword[16];
    uint32_t timestamp;
} AudioMessage;

typedef struct {
    float vision_score;
    uint8_t detection_type;  // 0=正常，1=冲突，2=聚集
    uint32_t timestamp;
} VisionMessage;

// 信号量
SemaphoreHandle_t xPostureSemaphore;
SemaphoreHandle_t xAudioSemaphore;
SemaphoreHandle_t xVisionSemaphore;
SemaphoreHandle_t xAlertSemaphore;

// 消息队列
QueueHandle_t xPostureQueue;
QueueHandle_t xAudioQueue;
QueueHandle_t xVisionQueue;
```

### 8.3 内存管理策略

```c
// 共享 Tensor Arena 管理
static uint8_t s_tensor_arena[512 * 1024];  // 512KB 共享区域
static bool s_arena_in_use = false;

// 获取 Tensor Arena
bool acquire_tensor_arena(void) {
    BaseType_t result = xSemaphoreTakeRecursive(xArenaSemaphore, pdMS_TO_TICKS(100));
    if (result == pdTRUE) {
        s_arena_in_use = true;
        return true;
    }
    return false;
}

// 释放 Tensor Arena
void release_tensor_arena(void) {
    s_arena_in_use = false;
    xSemaphoreGiveRecursive(xArenaSemaphore);
}

// 动态缓冲区管理
static HeapRegion_t heap_regions[] = {
    {
        .pucStartAddress = psram_start() + 1024 * 1024,  // 1MB PSRAM 用于动态分配
        .usLength = 7 * 1024 * 1024,                      // 7MB 可用
    }
};
```

---

## 9. 技术栈

### 9.1 嵌入式端

| 层级 | 技术 | 版本/说明 |
|-----|------|----------|
| **操作系统** | ESP-IDF / FreeRTOS | v5.1+ |
| **AI 推理** | TensorFlow Lite Micro | v2.12+ |
| **传感器驱动** | ESP-IDF I2C Driver | 原生驱动 |
| **音频处理** | ESP-IDF I2S Driver | 原生驱动 |
| **图像处理** | ESP-IDF CAM Driver | 原生驱动 |
| **通信协议** | LwIP | 原生 TCP/IP |
| **加密** | mbedTLS | TLS 1.3 |

### 9.2 云端

| 组件 | 技术 | 说明 |
|-----|------|------|
| **API 服务** | Node.js / Python | RESTful API |
| **数据库** | PostgreSQL + Redis | 结构化数据 + 缓存 |
| **消息队列** | RabbitMQ / Kafka | 警报分发 |
| **位置服务** | 自研三边定位 | BLE RSSI 融合 |
| **推送服务** | Firebase / 极光推送 | 多端通知 |

### 9.3 移动端

| 平台 | 技术 | 说明 |
|-----|------|------|
| **老师 APP** | Flutter | iOS + Android |
| **家长 APP** | Flutter | iOS + Android |
| **保安室大屏** | Vue.js + WebSocket | Web 应用 |

---

## 10. 开发规划（MVP 阶段）

### 10.1 阶段划分

| 阶段 | 周期 | 目标 | 交付物 |
|-----|------|------|--------|
| **P0** | 2 周 | 硬件选型验证 | 原理图、BOM 表 |
| **P1** | 4 周 | 基础驱动开发 | I2C/I2S/DCMI 驱动 |
| **P2** | 6 周 | 三重判定引擎 | 姿态 + 音频 + 视觉模型 |
| **P3** | 4 周 | 通信定位系统 | BLE + 4G + 定位算法 |
| **P4** | 4 周 | 云端服务开发 | API + 数据库 |
| **P5** | 4 周 | 移动端开发 | 老师/家长 APP |
| **P6** | 4 周 | 联调测试 | 完整系统 |

### 10.2 MVP 功能清单

| 功能 | 优先级 | 说明 |
|-----|-------|------|
| 姿态检测 | P0 | 核心功能，常开 |
| 麦克风关键词检测 | P0 | 核心功能，条件唤醒 |
| 摄像头视觉检测 | P1 | 可选功能，高功耗 |
| BLE 心跳广播 | P0 | 基础通信 |
| BLE 警报广播 | P0 | 群体协作 |
| 4G 数据上传 | P0 | 远程通知 |
| 位置计算 | P0 | BLE + 基站融合 |
| 家长控制 | P1 | 隐私配置 |
| 低电量管理 | P1 | 续航优化 |

---

## 11. 附录

### 11.1 术语表

| 术语 | 全称 | 说明 |
|-----|------|------|
| BLE | Bluetooth Low Energy | 低功耗蓝牙 |
| RSSI | Received Signal Strength Indicator | 接收信号强度指示 |
| DCMI | Digital Camera Memory Interface | 数字摄像头接口 |
| I2S | Inter-IC Sound | 集成电路音频总线 |
| I2C | Inter-Integrated Circuit | 集成电路总线 |
| TinyML | Tiny Machine Learning | 嵌入式机器学习 |

### 11.2 参考文档

- ESP32-S3 Technical Reference Manual
- QMA7981 Datasheet (Infineon)
- TensorFlow Lite Micro Documentation
- EC200U Hardware Design (Quectel)

### 11.3 版本历史

| 版本 | 日期 | 变更说明 |
|-----|------|----------|
| v0.1 | 2026-04-13 | 初始概念版本 |
| v0.2 | 2026-04-13 | 三重判定引擎版本 |
| **v0.3** | **2026-04-13** | **ESP32-S3-EYE 深度修正版本** |

---

**文档版本**: v0.3  
**最后更新**: 2026 年 4 月 13 日  
**作者**: BanSafe 开发团队  
**状态**: 草稿
